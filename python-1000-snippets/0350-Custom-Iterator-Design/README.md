# Custom Iterator Design

## Description
Shows how to implement custom iterator classes using `__iter__` and `__next__`.

## Concepts
- `__iter__` returns the iterator object
- `__next__` returns the next value or raises `StopIteration`
- Iterators can maintain internal state

## Output
Each sample iterates over a custom sequence and prints values.
