# 0373-Tree-Structure-Composite Cheatsheet

- Composite lets you treat individual objects and compositions uniformly.
- Components implement the same interface (e.g., `operation()`).
- The composite stores children and delegates operations to them.
- Useful for tree-like structures (UI, file systems, organization charts).
