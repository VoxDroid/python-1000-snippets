# 0365-Dynamic-Strategy-Selection Cheatsheet

- Strategy pattern defines a family of algorithms and selects them at runtime.
- Use a mapping from keys to strategy objects/functions.
- Strategies should share a common interface (method name).
- Keep strategy implementations small and testable.
