# 0384-Modular-Code-Structure Cheatsheet

- Use modules (`.py` files) to group related code.
- Import with `from module import name` or `import module`.
- Avoid circular imports (module A imports B and B imports A).
- Keep module dependencies minimal and well-defined.
