# 0344-Custom-Sorting-Algorithms Cheatsheet

- Use `sorted(iterable, key=...)` for custom sort keys.
- Use `functools.cmp_to_key` to adapt comparison functions.
- Simple sorts (selection, insertion) are educational but not production-grade.
