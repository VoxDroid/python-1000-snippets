# 0378-Algorithm-Template Cheatsheet

- Template method defines the skeleton of an algorithm.
- Subclasses override hook methods to customize behavior.
- Keep template method steps in a fixed order.
- Use `super()` if extending base behavior in subclasses.
