# 0372-Implementation-Bridge Cheatsheet

- Bridge decouples an abstraction from its implementation.
- Use separate hierarchies: abstraction (client-facing) and implementor (platform-specific).
- Allows changing implementations at runtime.
- Good for cross-platform or multi-API support.
