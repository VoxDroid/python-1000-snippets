# 0386-Parameterized-Unit-Tests Cheatsheet

- Use `pytest.mark.parametrize` to run a test with multiple input sets.
- Keep each test case focused and independent.
- Use fixtures for shared setup data.
- Combine parameterization with `pytest.raises` to test exceptions.
