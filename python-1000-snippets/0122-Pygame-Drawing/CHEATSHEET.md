# 0122-Pygame-Drawing Cheatsheet

- **Purpose**: draw primitives (rectangles, circles) using Pygame, with headless support.
- **Headless**: sets `SDL_VIDEODRIVER=dummy` and limits frames.
- **Usage**: run script after installing `pygame`.

```python
# just execute the README example
python 0122-Pygame-Drawing/README.md
```

- Replace shapes or colors with your own; wrap drawing in functions for reuse.
- Use `pygame.draw` methods: `rect`, `circle`, `line`, etc.

